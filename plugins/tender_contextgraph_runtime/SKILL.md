# Tender ContextGraph Runtime Expert

Read-only candidate graph inspection. Every request must retain tenant, workspace,
artifact, graph version/hash, mapper version, and Semantica commit pins.

This capability cannot mutate, admit, promote, approve, or certify graph data.
