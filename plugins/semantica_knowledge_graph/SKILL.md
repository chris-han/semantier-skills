# Semantica Knowledge Graph

Use the registered Semantier tools for governed ingestion, extraction,
deduplication, provenance, querying, validation, and visualization.

Do not import upstream Semantica packages directly. Do not parse arbitrary host
paths or parse arbitrary host paths from user input, and do not use raw HTTP;
use governed upload/artifact/source references. If the
tool surface is not loaded, fail closed and request an authenticated runtime
context. Candidate results are proposal-only and cannot create accepted graph
authority.
